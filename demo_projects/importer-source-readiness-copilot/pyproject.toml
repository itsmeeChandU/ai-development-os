[project]
name = "importer-source-readiness-copilot"
version = "0.1.0"
description = "Fixture-only importer/exporter source readiness copilot demo."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
